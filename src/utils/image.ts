/**
 * Get image path
 * Returns the path as-is for root deployment.
 */
export const getImgPath = (path: string): string => {
  return path;
};

/**
 * Get data file path
 * Returns the path as-is for root deployment.
 */
export const getDataPath = (path: string): string => {
  return path;
};
